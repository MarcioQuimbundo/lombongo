import 'package:flutter/material.dart';

void main() => runApp(Lombongo());

class Lombongo extends StatelessWidget {
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text('Lombongo'),
        ),
        body: Container(

        ),
      ),
    );
  }
}